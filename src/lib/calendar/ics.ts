/**
 * ICS (RFC 5545) renderer (R3b). Canonical code lives in
 * `supabase/functions/_shared/calendar.ts` (shared with the `ics-feed` edge function);
 * this module re-exports it for `@/lib/calendar`.
 */
export * from '../../../supabase/functions/_shared/calendar';
